# Environment variables example file for Vibe LMS
# Copy this file to .env and fill in the values

# REQUIRED VARIABLES
# PostgreSQL password (use a strong password!)
POSTGRES_PASSWORD=your_secure_password_here

# ADDITIONAL DEVELOPMENT VARIABLES
# (For Docker these variables are configured automatically)

# Database connection string
DATABASE_URL=postgresql://vibelms:your_password@localhost:5432/vibelms

# PostgreSQL connection parameters
PGHOST=localhost
PGPORT=5432
PGUSER=vibelms
PGPASSWORD=your_password
PGDATABASE=vibelms

# Runtime environment
NODE_ENV=production