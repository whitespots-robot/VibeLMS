# Пример файла переменных окружения для Vibe LMS
# Скопируйте этот файл в .env и заполните значения

# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ
# Пароль для PostgreSQL (используйте сложный пароль!)
POSTGRES_PASSWORD=your_secure_password_here

# ДОПОЛНИТЕЛЬНЫЕ ПЕРЕМЕННЫЕ ДЛЯ РАЗРАБОТКИ
# (Для Docker эти переменные настраиваются автоматически)

# Строка подключения к базе данных
DATABASE_URL=postgresql://vibelms:your_password@localhost:5432/vibelms

# Параметры подключения к PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=vibelms
PGPASSWORD=your_password
PGDATABASE=vibelms

# Среда выполнения
NODE_ENV=production